---
---
<section class="hero" id="hero">
  <div class="container hero-inner">
    <div class="hero-content">
      <span class="section-label">Ihr Partner für Gebäudeservice</span>
      <h1>Sauber. Sicher.<br /><span class="accent">Zuverlässig.</span></h1>
      <p class="hero-sub">
        FacilityPro GmbH bietet professionelles Facility Management für Unternehmen und Liegenschaften in ganz Deutschland — von der Reinigung bis zur technischen Wartung.
      </p>
      <div class="hero-actions">
        <a href="#termin" class="btn btn-primary">Jetzt Termin buchen →</a>
        <a href="#leistungen" class="btn btn-outline">Unsere Leistungen</a>
      </div>
      <div class="hero-stats">
        <div class="stat">
          <strong>250+</strong>
          <span>Zufriedene Kunden</span>
        </div>
        <div class="stat">
          <strong>15 Jahre</strong>
          <span>Erfahrung</span>
        </div>
        <div class="stat">
          <strong>24/7</strong>
          <span>Notfalldienst</span>
        </div>
      </div>
    </div>

    <div class="hero-visual">
      <div class="hero-card card-1">
        <span class="card-icon">✅</span>
        <span>Geprüfte Qualität</span>
      </div>
      <div class="hero-card card-2">
        <span class="card-icon">🇩🇪</span>
        <span>Frankfurt / Rhein-Main</span>
      </div>
      <div class="hero-blob"></div>
      <div class="hero-logo-card">
        <img src="/media/sanver-logo-hero-hd.png" alt="Sanver Facility Service" />
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: linear-gradient(135deg, var(--blue-900) 0%, var(--blue-700) 100%);
    color: var(--white);
    padding: 100px 0 80px;
    overflow: hidden;
    position: relative;
  }

  .hero-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: center;
  }

  .hero-content h1 {
    color: var(--white);
    font-size: clamp(2.2rem, 4.5vw, 3.5rem);
    font-weight: 800;
    margin-bottom: 20px;
    line-height: 1.1;
  }

  .accent { color: var(--yellow-400); }

  .hero-sub {
    color: rgba(255,255,255,0.8);
    font-size: 1.1rem;
    margin-bottom: 36px;
    max-width: 480px;
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    margin-bottom: 48px;
  }

  .hero-stats {
    display: flex;
    gap: 32px;
    padding-top: 32px;
    border-top: 1px solid rgba(255,255,255,0.15);
  }

  .stat strong {
    display: block;
    font-size: 1.6rem;
    font-weight: 800;
    color: var(--yellow-400);
  }

  .stat span {
    font-size: 0.85rem;
    color: rgba(255,255,255,0.6);
  }

  .hero-visual {
    position: relative;
  }

  .hero-logo-card {
    background: var(--white);
    border-radius: 20px;
    padding: 40px;
    box-shadow: var(--shadow-lg);
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .hero-logo-card img {
    width: 100%;
    max-width: 340px;
    height: auto;
    display: block;
  }

  .hero-blob {
    position: absolute;
    width: 300px;
    height: 300px;
    background: var(--yellow-400);
    opacity: 0.1;
    border-radius: 50%;
    top: -60px;
    right: -60px;
    filter: blur(40px);
  }

  .hero-card {
    position: absolute;
    display: flex;
    align-items: center;
    gap: 10px;
    background: var(--white);
    color: var(--blue-900);
    font-weight: 600;
    font-size: 0.85rem;
    padding: 10px 16px;
    border-radius: 100px;
    box-shadow: var(--shadow);
    z-index: 2;
    white-space: nowrap;
  }

  .card-1 { top: -16px; left: -16px; }
  .card-2 { bottom: 20px; right: -16px; }
  .card-icon { font-size: 1rem; }

  @media (max-width: 900px) {
    .hero-inner { grid-template-columns: 1fr; gap: 40px; }
    .hero-visual { max-width: 480px; margin: 0 auto; }
    .card-1 { top: -12px; left: 12px; }
    .card-2 { bottom: 12px; right: 12px; }
  }

  @media (max-width: 480px) {
    .hero-stats { gap: 20px; }
    .hero-actions { flex-direction: column; }
    .btn { justify-content: center; text-align: center; }
  }
</style>
